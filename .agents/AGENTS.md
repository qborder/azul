# Agent Guidelines for Azul Project

After modifying any files, always perform the following actions:

1. **Build Daemon**: Run `pnpm build` to compile TypeScript changes.
2. **Build Plugin**: Run `pnpm build:plugin` to build and install the companion plugin to Roblox Studio.
3. **Install Globally**: Run `pnpm install:g` to link the CLI globally.
